[project]
name = "cutting-stock"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "highspy>=1.14.0",
    "ortools>=9.15.6755",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["solver/tests"]
addopts = "-q"
